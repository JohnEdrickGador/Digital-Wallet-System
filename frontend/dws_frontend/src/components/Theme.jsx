import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f16125', // Custom color for primary buttons
    },
    secondary: {
      main: '#1577b2', // Custom color for secondary buttons
    },
  },
});

export default theme;
